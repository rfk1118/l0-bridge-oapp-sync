{
  "name": "l0-bridge-oapp-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub Actions cron that syncs the LayerZero V2 OApp index for l0-bridge.",
  "scripts": {
    "sync": "tsx scripts/sync.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ethers": "^6.15.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  }
}
